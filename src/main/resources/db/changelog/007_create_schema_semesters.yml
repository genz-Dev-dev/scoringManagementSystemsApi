databaseChangeLog:
    - changeSet:
          id: 007_create_semesters_table
          author: tola.dev001@gmail.com
          changes:
              - createTable:
                    tableName: tbl_semester
                    columns:
                        - column:
                              name: semester_id
                              type: uuid
                              defaultValueComputed: gen_random_uuid() # 🔥 auto-generate UUID
                              constraints:
                                  primaryKey: true
                                  nullable: false
                        - column:
                              name: semester_name
                              type: varchar(50)
                              constraints:
                                  nullable: false
